<!DOCTYPE html>
<html>

<head>
	<title>Greeting</title>
</head>

<body>
	<% if (it.name) { %>
		<!-- Show greeting if the name is available -->
		<p>Hi <%= it.name %>!</p>
		<% } else { %>
			<!-- Show the form if the name is not available -->
			<p>Add a name:</p>
			<% if (it.error) { %>
				<p style="color: red;">
					<%= it.error %>
				</p>
				<% } %>
					<form method="POST" action="/">
						<label for="name">Type in your name:</label>
						<input type="text" id="name" name="name" /><br />
						<input type="submit" value="Submit" />
					</form>
					<% } %>
</body>

</html>